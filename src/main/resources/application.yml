spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studs
    username: s339795
    password: FH91Ze1WDQJk6Oed
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

server:
  port: 8081

RABBITMQ_HOST: "tcp://5.23.55.30:1883"